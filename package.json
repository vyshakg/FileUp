{
  "private": true,
  "scripts": {
    "build:app": "lerna run build --scope={@fileup/server,@fileup/view}",
    "start": "node build/index.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "name": "fileup",
  "devDependencies": {
    "lerna": "^3.13.0"
  }
}
