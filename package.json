{
  "private": true,
  "scripts": {
    "build:app": "lerna run build --scope={@fileup/server,@fileup/view}"
  },
  "workspaces": [
    "packages/*"
  ],
  "name": "fileup",
  "devDependencies": {
    "lerna": "^3.13.0"
  }
}
